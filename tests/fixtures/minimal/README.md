# Fixture provenance

`minimal.kicad_pcb` is a deliberately tiny KiCad 10 board used for CLI and live IPC smoke tests. It contains one two-pad footprint and a rectangular board outline. Tests copy it to a temporary/project-scoped location before any mutation.
