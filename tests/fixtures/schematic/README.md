# Schematic fixture

These files are copied from the KiCad 10.0.6 AppImage demo `simulation/power_supplies/boost` and are used as a separate schematic/ERC fixture suite. Upstream: KiCad source tag `10.0.6`, commit `caf7377e9cb6fa1535ec3596dcb8c99bf44a996e`. License: GPL-3.0-or-later. The runtime KiCad 11 IPC tests use fixture copies and remain version-gated.
